# ignore everything (dirs, files, sub-dirs, sub-files)
/*
# but do not ignore the .gitignore file
!.gitignore

# do not ignore .c files, Makefile and libft.h
!/src
!/src/*.c
!/wip
!/wip/*.c
!/Makefile
!/include
!/include/libft.h
